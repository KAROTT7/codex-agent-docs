# AGENTS.md

本文件是个人全局协作总则，适用于参与的各类项目。分类技术规范位于同目录的 `agent-docs/`，按需阅读与当前任务直接相关的文件。

## 1. 作用范围与优先级

- 本文件提供跨项目默认规则；项目及其子目录中的 `AGENTS.md` 可增加或覆盖对应范围的规则。
- 在全局与项目规范之间，优先遵循用户当前要求、项目内适用的 `AGENTS.md`（更深层级优先）及项目真实配置；文档与配置冲突时核实当前有效配置并报告差异。
- 默认技术约定用于新建项目或相关功能建设；已有项目优先沿用现有方案，不因局部修改顺手迁移配置或补齐无关依赖。
- 先理解项目形态、包管理器、脚本、技术选型和目录归属，再开始修改。

## 2. 分类规范入口

- 前端总则：[agent-docs/frontend.md](agent-docs/frontend.md)
- 后端总则：[agent-docs/backend.md](agent-docs/backend.md)

## 3. 通用工作方式

- 默认使用中文沟通；代码、注释、文档和提交信息遵循项目现有语言风格。
- 优先复用现有组件、工具、类型、接口和依赖，不为小需求引入新库或进行无关重构。
- 涉及接口、数据结构、数据库或服务端逻辑时，先查看类型、DTO、schema、实体和接口约定。
- 涉及 UI 时，先查看现有设计系统、主题变量和组件封装层。
- 如无明确说明，字段不额外添加兜底逻辑；前端接口失败时的展示数据兜底按前端总则执行，不改变既有接口契约。

## 4. 质量与验证

- 任何功能改动都执行与改动规模匹配的最小必要验证。
- 前端优先检查改动相关的 lint、typecheck、测试或 build；后端优先编译、单元测试和关键接口冒烟。
- 行为变更优先利用现有测试体系补充关键验证，包括 bug 回归、功能关键路径和异常分支；纯文档、文案或样式调整执行对应检查，不为小改动单独引入测试框架。
- 必须如实汇报执行过的命令、关键结果和未覆盖项，不声称未执行的检查已通过。

## 5. Git 规范

- 修改前先查看 `git status`、当前分支和相关文件，避免覆盖用户现场。
- 保留用户已有改动，只修改与当前任务相关的文件。
- 不使用 `git reset --hard`、`git checkout --`、强制推送等破坏性命令，除非用户明确要求。
- 不主动提交；只有用户明确要求时才创建 commit，且保持提交粒度清晰。
- commit message 简洁表达“做了什么 + 为什么”，末尾附加当前实际模型和提供商的 `Co-Authored-By` trailer。

## 6. 规范文件与本机工具

- 分发或部署全局规范时，将本文件与 `agent-docs/` 一起放置，保持相对目录结构；文档链接相对于所在规范文件解析，不相对于当前业务仓库。
- 执行终端命令前，读取 `~/.codex/RTK.md`（`~` 表示当前用户主目录）并遵循其中的命令约定；该文件或 `rtk` 不可用时，说明情况并使用原生命令继续，不自动安装工具或修改本机配置。
