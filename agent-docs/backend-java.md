# Java 后端

- 遵循项目现有 Java 版本、框架、Maven 或 Gradle 及模块结构。
- 请求对象、响应对象和数据库实体尽量分离，不直接暴露 Entity。
- 事务边界放在 Service 层；使用 `java.time`，金额等精度敏感数据使用 `BigDecimal`。
- 关注分页、索引、批量操作、N+1、并发、锁、幂等和缓存一致性。
- 复用统一返回体、异常处理、参数校验、对象转换和日志规范；日志不得输出 token、密码或完整敏感身份信息。
